#include "shader.h"
#include "render.h"

// INFO : I dont know if this function could be used on other API
u32 fn_getShaderIndex(E_primitiveType primitiveType)
{
        u32 returnValue = DEFAULT_SHADER_INDEX;
        return returnValue;
}
